<div class="outer-flex-container">
  <div class="container">

    <%
      @hero = {
        title: 'Edit Clock In',
        description: sanitize('Use the form below to edit clock event #' + @clockin.id.to_s + '. <a href="/" class="hero__link">Click here</a> to go home.'),
      }
    %>
    <%= render 'hero' %>

    <%= render 'edit_form' %>
  
  </div>
</div>